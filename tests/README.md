## Test *Blocky-list-updater*

The tests are utilizing [shellspec](https://github.com/shellspec/shellspec) framework. A quick installation of `shellspec` can be done by `curl -fsSL https://git.io/shellspec | sh`. See their website for more installation options.

Use the following commands to run all the tests locally. 

```
shellspec -s bash
```